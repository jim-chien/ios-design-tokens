# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'
target 'ios-design-tokens' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for ios-design-tokens
  pod 'MaterialComponents/Buttons'
  pod 'my-design-tokens', :git => "https://github.com/jim-chien/my-design-tokens.git", :tag => 'v1.0.5'

end
